# dicom_analyzer
 DICOM File Security Analyzer DICOM is the standard format for medical images (MRIs, X-rays). Build a tool that checks DICOM files for embedded patient metadata (PHI) that could be a privacy leak, and optionally strips or encrypts it.
